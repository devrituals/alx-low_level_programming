#include "main.h"

/**
 * _isalpha-execution entry point
 * @c: this evaluate the given data inorder to return aresult
 * Return:1 if success or 0 if failure
 */

int _isalpha(int c)
{
	return ((c >= 65 && c <= 122));
}
